# Change 005 internal expert-validation session

Status: prepared; scoring not started.

Evaluator role: project owner, journal editor, scientific author, and physics-domain specialist.

## Interpretation

This is a formal internal expert evaluation of the MVP. It can identify defects and support an internal pilot decision, but it is not described as blind, independent, or externally validated. A colleague will later conduct a separate external round without access to the internal scores.

## Protocol

- Twelve rights-cleared cases use IDs `JM-INT-001` through `JM-INT-012`.
- The evaluator records provenance, journal, article type, format, six ordinal scores, seven binary critical-safety checks, and supporting comments.
- Frozen internal gate: all 12 cases and 72 scores completed; zero critical failures; median at least 4 in every dimension; at least 80% of usefulness and actionability scores at least 4.
- Passing is labeled `INTERNAL PASS`; it does not satisfy the external-review or staging gates and does not authorize Change 006 by itself.

The controlled workbook is stored under `outputs/internal-validation-c5/`. Completed scores and artifact evidence must be preserved as a new immutable evaluation record before any system fixes are applied.
